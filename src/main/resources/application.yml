spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/uterm
    username: utermadmin
    password: admin1234
  devtools:
    livereload:
      enalbes: true
  freemarker:
    cache: false

uterm:
  domain:
    name: "localhost:8999"

server:
  port: 8999
  error:
    whitelabel:
      enabled: false;